const message = require("./message")//crear Variable con el mismo nombre del archivo a incluir y enrte "" el path donde se encunetra el archivo SIN .js

let message = "Acámica";

console.log(message);